[project]
name = "proy"
version = "0.1.0"
description = ""
authors = [
    {name = "Alejrot"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow (>=11.2.1,<12.0.0)",
    "rich (>=14.0.0,<15.0.0)",
    "python-i18n[yaml] (>=0.3.9,<0.4.0)"
]

[tool.poetry]
package-mode = false
packages = [{include = "proy", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
